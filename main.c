#include <stdio.h>

#include "life.h"

int main( int argc, char **argv){

    // plife state;
    // int n_gen = 6;

    // for (int n = 0; n < n_gen; n++) {
    //     plife new_state = generate_life(state);

    //     // zapis

    //     free_life(state);
    //     state = new_state;
    // }

    return 0;
}